# Staff App

Nuxt staff console for the IAM Agent demo.

This app is set up with:

- Nuxt 4
- Tailwind CSS
- shadcn-vue
- pnpm

## Setup

Install dependencies:

```bash
pnpm install
```

Run the local dev server:

```bash
pnpm dev
```

Build for production:

```bash
pnpm build
```
